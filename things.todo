modify the log to use zgrep when the logfile is compressed - DONE, also added live option using tail -f
setup the logrotator for simplewallet.log
add log view for redis
add case instance for all input text variables for back and help
create help string that prints out help commands
add a function to list available wallets and create a directory of wallets with names and current balances

switch executing bitmonerod with flags to the conf file, if the conf file works
